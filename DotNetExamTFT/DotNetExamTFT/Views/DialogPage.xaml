<Page
    x:Class="DotNetExamTFT.Views.DialogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DotNetExamTFT.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBox Margin="10" VerticalAlignment="Center" PlaceholderText="Enter the units name" Text="{x:Bind ViewModel.Name, Mode=TwoWay}" Style="{ThemeResource TitleTextBlockStyle}" />
        <TextBox Margin="10" VerticalAlignment="Center" PlaceholderText="Enter cost " Text="{x:Bind ViewModel.Cost, Mode=TwoWay}" Style="{ThemeResource TitleTextBlockStyle}" />
        <TextBox Margin="10" VerticalAlignment="Center" PlaceholderText="Enter mana" Text="{x:Bind ViewModel.Mana, Mode=TwoWay}" Style="{ThemeResource TitleTextBlockStyle}" />
    </StackPanel>
</Page>
